# Generated daily by `rightsize refresh`. Public catalogue data, no secrets,
# but it churns every day so it is not worth versioning.
registry.json

# Infisical link file: carries the project id for this machine.
.infisical.json

# Local logs from the daily job.
daily.log

__pycache__/
*.pyc
.DS_Store

# Orca child worktrees land inside this repo (--worktree new-child), so a
# blanket `git add -A` picks them up as gitlinks. They are checkouts, not content.
